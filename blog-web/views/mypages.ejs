<%- include header %>

<div id="content-main-page">

<% if(user){ %>

<p class="passage_note">notes</p>
<div class="passage_box">
  <% posts.forEach(function (post, index) { %>
        <div class="post-section post-section-2">
            <div>
            <p>
            <a href="/single/<%= post.title%>" class="a_title">
            <%=post.title %>
          </a></br> title
            <a href="#">
            <%=post.name %>
          </a> | post
            <%=post.time.minute %>
            </p>
            </div>
            <div>
            <p>
           <%- post.post.substring(1,100).replace(/<br\/>/g,'') %>......
            <a href="/post/<%= post.title%>"+ class="read_more">Read More</a>
            </p>
            </div>

        </div>
        <% }) %>
        <div style="clear:both"></div>
</div>

</div>
<% } %>
</div>
<%- include footer %>
